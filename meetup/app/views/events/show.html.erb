<style>
h1{
  text-align: center;
}
</style>

<p id="notice"><%= notice %></p>


<div class="container-fluid">
  <div class="jumbotron" style="background-image: url('<%= @event.ev_image %>'); width: 100%;height:300px;no-repeat">
  </div>
</div> 

<div class="container">
  <h1 class="text-primary"><%= @event.name %></h1>
  <h1>Number of Attendens : <%= @num_attendens %></h1>


  <% if @flag_attend==1 %>
  <div class="col-md-4" ><%= link_to 'Attend', {:controller => "events", :action => "attend", :user_id => current_user.id, :event_id => @event.id }, class:"btn btn-success" %></div>
  <% end %>

        <h2>
          Date : <%= @event.date %>
        </h2>
        <h2>
          Location :  <%= @event.location %>
        </h2>
        <h2>
          Description :  <%= @event.desc %>
        </h2>     
          Comments







<table>
          <% @comments.each do |comment| %>
      <tr>
        <td><%= comment.body %></td>
        <td><%= comment.user_id %></td>
      </tr>
      <% end %>

        

      
 </table>

<input type="text"  id="comment"/>
<input type="button"  id="btn" value="btn"/>

 <script type="text/javascript"> 
 
 $( "#btn" ).click(function(){
  var user_id = 1;
  var body = document.getElementById("comment").value;
  var event_id = 2;
  var posting = $.post( "/comments.json", { body: body, user_id: user_id , event_id: event_id} , function( data ) {
  alert( "lol"); // John
}, "json");




 

  });

</script>

<% if logged_in? %>

<%= link_to 'Edit', edit_event_path(@event) %> 

<% end %>
   
  </div>  



